import { passageDefaults, storyDefaults } from "../../stories";
import { Passage, Story } from '../../stories';

/**
 * Parses initial state from local storage.
 */
export async function load(): Promise<Story[]> {

	// Flatten the stories object.
	return ;
}
